language: cpp
os:
  - linux
sudo: required
dist: xenial
compiler:
  - g++
  - clang
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install libibus-1.0-dev qt5-default rustc cargo
before_script:
  - mkdir build
  - cd build
  - cmake ..
script:
  - make
